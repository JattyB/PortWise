# Local CVE corpus

The packaged subset uses NVD CVE JSON 2.0 field names and CPE 2.3 match
criteria. Records are selected for deterministic offline regression and are
updated from NVD source records. A sync process may replace or extend this file
without changing the matcher.
